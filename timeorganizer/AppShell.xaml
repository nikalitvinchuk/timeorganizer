<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="timeorganizer.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:timeorganizer"
    xmlns:views="clr-namespace:timeorganizer.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent
    Title="Home"
    ContentTemplate="{DataTemplate views:RegisterPage}"
    Route="RegisterPage" />
    <!--<Shell.Resources>
    <ResourceDictionary>
        --><!-- Definicja kolorów --><!--
        <Color x:Key="PrimaryColor">#3498db</Color>
        <Color x:Key="SecondaryColor">#2ecc71</Color>

        --><!-- Definicja stylu dla Label -->
        <!--<Style TargetType="Label" x:Key="CustomLabelStyle">
            <Setter Property="TextColor" Value="{StaticResource PrimaryColor}" />
            <Setter Property="FontSize" Value="16" />
        </Style>--><!--
    </ResourceDictionary>
    </Shell.Resources>-->

</Shell>
